# Port the Express server listens on
PORT=5000

# MongoDB connection string — local mongod or an Atlas free-tier cluster
MONGODB_URI=mongodb://127.0.0.1:27017/skillpath-ai

# Origin allowed by CORS (the Next.js app). Only relevant for requests that
# don't go through the Next.js rewrite proxy (e.g. direct API calls, Stripe CLI).
CLIENT_ORIGIN=http://localhost:3000
